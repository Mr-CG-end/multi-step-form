export interface IContent {
  id: string;
  title: string;
  semititle: string;
}

export interface ITab {
  id: string;
  step: string;
  name: string;
}

export interface ITabMeta {
  id: string;
}
